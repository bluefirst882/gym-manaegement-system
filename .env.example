# 复制本文件为 .env 并填入真实值；.env 不会被提交到 git
DB_HOST=localhost
DB_PORT=3306
DB_NAME=extreme_sports_venue
DB_USERNAME=root
DB_PASSWORD=

# 请替换为随机值：openssl rand -base64 48
JWT_SECRET=
JWT_EXPIRATION=86400000

SERVER_PORT=8080
