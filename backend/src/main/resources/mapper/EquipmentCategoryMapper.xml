<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.limitless.backend.mapper.EquipmentCategoryMapper">

    <select id="selectById" resultType="EquipmentCategory">
        SELECT * FROM equipment_category WHERE id = #{id}
    </select>

    <select id="selectPage" resultType="EquipmentCategory">
        SELECT * FROM equipment_category
        <where>
            <if test="categoryName != null and categoryName != ''">
                AND category_name LIKE CONCAT('%', #{categoryName}, '%')
            </if>
        </where>
        ORDER BY id ASC
    </select>

    <select id="countPage" resultType="long">
        SELECT COUNT(*) FROM equipment_category
        <where>
            <if test="categoryName != null and categoryName != ''">
                AND category_name LIKE CONCAT('%', #{categoryName}, '%')
            </if>
        </where>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO equipment_category (category_code, category_name, category_desc)
        VALUES (#{categoryCode}, #{categoryName}, #{categoryDesc})
    </insert>

    <update id="updateById">
        UPDATE equipment_category
        <set>
            <if test="categoryName != null">category_name = #{categoryName},</if>
            <if test="categoryDesc != null">category_desc = #{categoryDesc},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM equipment_category WHERE id = #{id}
    </delete>
</mapper>
