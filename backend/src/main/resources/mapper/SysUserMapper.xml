<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.limitless.backend.mapper.SysUserMapper">

    <resultMap id="userMap" type="SysUser">
        <id property="id" column="id"/>
        <result property="roleName" column="role_name"/>
    </resultMap>

    <select id="selectById" resultMap="userMap">
        SELECT u.*, r.role_name
        FROM sys_user u
        LEFT JOIN sys_role r ON u.role_id = r.id
        WHERE u.id = #{id}
    </select>

    <select id="selectByUsername" resultMap="userMap">
        SELECT u.*, r.role_name
        FROM sys_user u
        LEFT JOIN sys_role r ON u.role_id = r.id
        WHERE u.username = #{username}
    </select>

    <select id="selectPage" resultMap="userMap">
        SELECT u.*, r.role_name
        FROM sys_user u
        LEFT JOIN sys_role r ON u.role_id = r.id
        <where>
            <if test="username != null and username != ''">
                AND u.username LIKE CONCAT('%', #{username}, '%')
            </if>
            <if test="realName != null and realName != ''">
                AND u.real_name LIKE CONCAT('%', #{realName}, '%')
            </if>
            <if test="phone != null and phone != ''">
                AND u.phone LIKE CONCAT('%', #{phone}, '%')
            </if>
            <if test="roleId != null">
                AND u.role_id = #{roleId}
            </if>
        </where>
        ORDER BY u.created_at DESC
    </select>

    <select id="countPage" resultType="long">
        SELECT COUNT(*)
        FROM sys_user u
        <where>
            <if test="username != null and username != ''">
                AND u.username LIKE CONCAT('%', #{username}, '%')
            </if>
            <if test="realName != null and realName != ''">
                AND u.real_name LIKE CONCAT('%', #{realName}, '%')
            </if>
            <if test="phone != null and phone != ''">
                AND u.phone LIKE CONCAT('%', #{phone}, '%')
            </if>
            <if test="roleId != null">
                AND u.role_id = #{roleId}
            </if>
        </where>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (username, password, real_name, phone, gender, birthday, role_id, status)
        VALUES (#{username}, #{password}, #{realName}, #{phone}, #{gender}, #{birthday}, #{roleId}, #{status})
    </insert>

    <update id="updateById">
        UPDATE sys_user
        <set>
            <if test="realName != null">real_name = #{realName},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="birthday != null">birthday = #{birthday},</if>
            <if test="roleId != null">role_id = #{roleId},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM sys_user WHERE id = #{id}
    </delete>

    <update id="updatePassword">
        UPDATE sys_user SET password = #{password} WHERE id = #{id}
    </update>

    <update id="updateLastLogin">
        UPDATE sys_user SET last_login_at = NOW() WHERE id = #{id}
    </update>

</mapper>
