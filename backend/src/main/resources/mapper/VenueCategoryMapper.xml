<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.limitless.backend.mapper.VenueCategoryMapper">

    <select id="selectById" resultType="VenueCategory">
        SELECT id, category_code, category_name, category_desc, sort_order, created_at, updated_at
        FROM venue_category WHERE id = #{id}
    </select>

    <select id="selectPage" resultType="VenueCategory">
        SELECT id, category_code, category_name, category_desc, sort_order, created_at, updated_at
        FROM venue_category
        <where>
            <if test="categoryName != null and categoryName != ''">
                AND category_name LIKE CONCAT('%', #{categoryName}, '%')
            </if>
        </where>
        ORDER BY sort_order ASC
    </select>

    <select id="countPage" resultType="long">
        SELECT COUNT(*) FROM venue_category
        <where>
            <if test="categoryName != null and categoryName != ''">
                AND category_name LIKE CONCAT('%', #{categoryName}, '%')
            </if>
        </where>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO venue_category (category_code, category_name, category_desc, sort_order)
        VALUES (#{categoryCode}, #{categoryName}, #{categoryDesc}, #{sortOrder})
    </insert>

    <update id="updateById">
        UPDATE venue_category
        <set>
            <if test="categoryName != null">category_name = #{categoryName},</if>
            <if test="categoryDesc != null">category_desc = #{categoryDesc},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM venue_category WHERE id = #{id}
    </delete>

    <select id="countVenueByCategoryId" resultType="int">
        SELECT COUNT(*) FROM venue WHERE category_id = #{categoryId}
    </select>

</mapper>
